#------------------------------------------------#
# 通用容器配置
#------------------------------------------------#

# 镜像版本
IMAGE_VERSION=v1
# 容器名称
CONTAINER_NAME=homepage

# 主机绑定IP (0.0.0.0 表示监听所有IP)
HOST_IP=0.0.0.0
# 主机暴露端口
HOST_PORT=13000

# 容器重启策略
RESTART_POLICY=unless-stopped
# CPU核心数限制
CPU_LIMIT=1
# 内存大小限制
MEMORY_LIMIT=1G

#------------------------------------------------#
# Homepage 特有配置
#------------------------------------------------#

# 用户ID (宿主机用户ID，用于文件权限)
PUID=1000
# 用户组ID (宿主机用户组ID，用于文件权限)
PGID=1000
# 时区
TZ=Asia/Shanghai
