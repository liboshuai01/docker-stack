#------------------------------------------------#
# 通用容器配置
#------------------------------------------------#

# 镜像版本
IMAGE_VERSION=1.28.0
# 容器名称
CONTAINER_NAME=nginx

# 主机绑定IP (0.0.0.0 表示监听所有IP)
HOST_IP=0.0.0.0
# 主机暴露端口
HOST_PORT=80

# 容器重启策略
RESTART_POLICY=unless-stopped
# CPU核心数限制
CPU_LIMIT=1
# 内存大小限制
MEMORY_LIMIT=1G

#------------------------------------------------#
# Nginx 特有配置 (如果需要，可在此添加)
#------------------------------------------------#

# 示例：将来如果需要Nginx的特定环境变量，可以在这里添加，例如：
# NGINX_WORKER_PROCESSES=auto