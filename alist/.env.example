#------------------------------------------------#
# 通用容器配置
#------------------------------------------------#

# 镜像版本
IMAGE_TAG=latest
# 容器名称
CONTAINER_NAME=alist

# 主机绑定IP (0.0.0.0 表示监听所有IP)
HOST_IP=0.0.0.0
# 主机暴露端口
HOST_PORT=25244

# 容器重启策略
RESTART_POLICY=unless-stopped
# CPU核心数限制
CPU_LIMIT=1
# 内存大小限制
MEMORY_LIMIT=1G

#------------------------------------------------#
# OpenList 特有配置
#------------------------------------------------#

# 用户ID (通常为0，表示root用户)
PUID=0
# 组ID (通常为0，表示root组)
PGID=0
# 文件权限掩码
UMASK=022
# 时区
TZ=Asia/Shanghai
